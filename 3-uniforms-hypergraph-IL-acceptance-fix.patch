diff --git aa/.github/workflows/enumerate-smoke.yml bb/.github/workflows/enumerate-smoke.yml
index 0209696..2dad488 100644
--- aa/.github/workflows/enumerate-smoke.yml
+++ bb/.github/workflows/enumerate-smoke.yml
@@ -8,16 +8,19 @@ jobs:
   test:
     runs-on: ubuntu-latest
     steps:
-      - uses: actions/checkout@v4
-      - uses: actions/setup-python@v5
+      - uses: actions/checkout@v7
+      - uses: actions/setup-python@v7
         with:
           python-version: "3.12"
           cache: pip
-      - run: python -m pip install --upgrade pip
-      - run: python -m pip install -r requirements.txt
-      - run: python -m pip install -e . --no-build-isolation
+      - run: python -m pip install --upgrade pip setuptools wheel
+      - run: python -m pip install -e ".[test]" --no-build-isolation
       - run: python -m compileall -q src enumerate tools tests
       - run: python -m pytest -q
       - run: python tools/check_repository.py
       - run: python tools/check_generated_artifacts.py
-      - run: python enumerate/q0015_configuration_auditor.py --regressions-only --generated-at 2026-07-28T00:00:00Z --output-dir artifacts/runs/q0015
+      - run: >-
+          python enumerate/q0015_configuration_auditor.py
+          --regressions-only
+          --generated-at 2026-07-28T00:00:00Z
+          --output-dir artifacts/runs/q0015
diff --git aa/DELIVERY_MANIFEST.json bb/DELIVERY_MANIFEST.json
index 0194561..a62166a 100644
--- aa/DELIVERY_MANIFEST.json
+++ bb/DELIVERY_MANIFEST.json
@@ -6,9 +6,9 @@
   "archive_layout": "repository-root-direct",
   "contains_git_database": false,
   "workflow_protocol": {
-    "generic": "agent.md",
-    "project_specific": "AGENTS.md",
-    "explanatory": "WORKFLOW.md"
+    "mandatory_short_protocol": "agent.md",
+    "detailed_manual": "WORKFLOW.md",
+    "project_specific": "AGENTS.md"
   },
   "canonical_layers": {
     "knowledge": "knowledge/",
@@ -24,25 +24,31 @@
     "Q-0017": "open",
     "canonical_status_promoted_by_restructure": false
   },
-  "validation": {
-    "compileall": "passed",
-    "pytest": "14 passed",
-    "repository_consistency": "passed",
-    "generated_artifacts": "passed",
-    "q0015_regression": {
-      "root_groups_with_failures": 144,
-      "classification": {
-        "zero-error-budget-feasible": 48,
-        "positive-root-budget-gap": 48,
-        "no-configuration": 48
-      },
-      "specified_t_min": 2.0,
-      "forbidden_genealogy_merge_eta": 1.0,
-      "payload_sha256": "ef02bf6446618026052c1a107913e4d015e3c8f3b48a59441a72afa09429a8ad"
-    }
-  },
   "known_raw_source_limitations": [
     "One optional large historical conversation is referenced in sources/raw/MANIFEST.json but not embedded.",
     "The byte-for-byte upstream first-execution JSON is not embedded; a historical compatibility summary and reproducible current baseline are included."
-  ]
+  ],
+  "validation": {
+    "local_patch_validation": {
+      "compileall": "passed",
+      "pytest": "14 passed",
+      "repository_consistency": "passed",
+      "generated_artifacts": "passed",
+      "q0015_regression": {
+        "root_groups_with_failures": 144,
+        "classification": {
+          "zero-error-budget-feasible": 48,
+          "positive-root-budget-gap": 48,
+          "no-configuration": 48
+        },
+        "specified_t_min": 2.0,
+        "forbidden_genealogy_merge_eta": 1.0,
+        "payload_sha256": "ef02bf6446618026052c1a107913e4d015e3c8f3b48a59441a72afa09429a8ad"
+      }
+    },
+    "github_actions": {
+      "status": "must-pass-after-merge",
+      "workflow": ".github/workflows/enumerate-smoke.yml"
+    }
+  }
 }
diff --git ab/FACTS.md bb/FACTS.md
new file mode 100644
index 0000000..8095e22
--- /dev/null
+++ bb/FACTS.md
@@ -0,0 +1,3 @@
+# Compatibility pointer
+
+Canonical content: [`knowledge/FACTS.md`](knowledge/FACTS.md).
diff --git ab/FAILURES.md bb/FAILURES.md
new file mode 100644
index 0000000..e1a705a
--- /dev/null
+++ bb/FAILURES.md
@@ -0,0 +1,3 @@
+# Compatibility pointer
+
+Canonical content: [`knowledge/FAILURES.md`](knowledge/FAILURES.md).
diff --git ab/PIVOT_SWITCH_ESCAPE_FRAMEWORK.md bb/PIVOT_SWITCH_ESCAPE_FRAMEWORK.md
new file mode 100644
index 0000000..22cd42b
--- /dev/null
+++ bb/PIVOT_SWITCH_ESCAPE_FRAMEWORK.md
@@ -0,0 +1,5 @@
+# Compatibility pointer
+
+Historical monolith: [`history/legacy-frameworks/pivot-switch-escape-monolith.md`](history/legacy-frameworks/pivot-switch-escape-monolith.md).
+
+Normative module: [`docs/framework/FW-30_PIVOT_SWITCH_ESCAPE.md`](docs/framework/FW-30_PIVOT_SWITCH_ESCAPE.md).
diff --git ab/QUESTIONS.md bb/QUESTIONS.md
new file mode 100644
index 0000000..c3741ce
--- /dev/null
+++ bb/QUESTIONS.md
@@ -0,0 +1,3 @@
+# Compatibility pointer
+
+Canonical content: [`knowledge/QUESTIONS.md`](knowledge/QUESTIONS.md).
diff --git aa/README.md bb/README.md
index 204285c..d83acc7 100644
--- aa/README.md
+++ bb/README.md
@@ -1,11 +1,11 @@
 # Independent transversals in partitioned 3-uniform hypergraphs
 
-This repository studies the independent-transversal threshold for equal-block partitioned 3-uniform hypergraphs, with the target implication
+This repository studies the independent-transversal threshold for equal-block
+partitioned 3-uniform hypergraphs, with the target implication
 
 \[
-\Delta(H)<\left(rac14-o(1)
-ight)b^2
-\Longrightarrow H	ext{ has an independent transversal.}
+\Delta(H)<\left(\frac14-o(1)\right)b^2
+\Longrightarrow H\text{ has an independent transversal.}
 \]
 
 ## Current status
@@ -17,14 +17,16 @@ The one-quarter theorem is **open**. The only active main-proof node is:
 The current logical path is
 
 \[
-	ext{configuration entrance}
-	o 	ext{persistent-blocker normal form}
-	o 	ext{causal regeneration}
-	o 	ext{terminal counting}
-	o rac14.
+\text{configuration entrance}
+\to \text{persistent-blocker normal form}
+\to \text{causal regeneration}
+\to \text{terminal counting}
+\to \frac14.
 \]
 
-A formal future-complete lift is available after an actual Q-0015 root obstacle has already been constructed, but it does not provide the general entrance or downstream concentration theorem.
+A formal future-complete lift is available after an actual Q-0015 root obstacle
+has already been constructed, but it does not provide the general entrance or
+downstream concentration theorem.
 
 ## Ten-minute start
 
@@ -32,14 +34,22 @@ A formal future-complete lift is available after an actual Q-0015 root obstacle
 2. [`HANDOFF_CURRENT.md`](HANDOFF_CURRENT.md)
 3. [`docs/PROOF_DAG.md`](docs/PROOF_DAG.md)
 4. [`AGENTS.md`](AGENTS.md)
-5. [`knowledge/FACTS.md`](knowledge/FACTS.md)
-6. [`knowledge/FAILURES.md`](knowledge/FAILURES.md)
-7. [`knowledge/QUESTIONS.md`](knowledge/QUESTIONS.md)
+5. [`agent.md`](agent.md)
+6. [`knowledge/FACTS.md`](knowledge/FACTS.md)
+7. [`knowledge/FAILURES.md`](knowledge/FAILURES.md)
+8. [`knowledge/QUESTIONS.md`](knowledge/QUESTIONS.md)
+
+## Protocol split
+
+- `agent.md`: short mandatory write protocol;
+- `WORKFLOW.md`: detailed explanations, templates, and examples;
+- `AGENTS.md`: project-specific mathematical and repository constraints.
+
+Agents should read the short protocol on every write task and load the detailed
+workflow only when its templates or explanations are needed.
 
 ## Information architecture
 
-- `agent.md`: reusable repository-write protocol;
-- `AGENTS.md`: project-specific constraints;
 - `knowledge/`: canonical facts, failures, questions, decisions, and definitions;
 - `docs/framework/`: normative mathematical interfaces;
 - `evidence/`: audits, experiment baselines, reports, proofs, and certificates;
@@ -51,15 +61,20 @@ A formal future-complete lift is available after an actual Q-0015 root obstacle
 ## Install and validate
 
 ```bash
-python -m pip install -r requirements.txt
-python -m pip install -e . --no-build-isolation
+python -m pip install --upgrade pip setuptools wheel
+python -m pip install -e ".[test]" --no-build-isolation
 python -m compileall -q src enumerate tools tests
 python -m pytest -q
 python tools/check_repository.py
 python tools/check_generated_artifacts.py
-python enumerate/q0015_configuration_auditor.py --regressions-only --output-dir artifacts/runs/q0015
+python enumerate/q0015_configuration_auditor.py \
+  --regressions-only \
+  --generated-at 2026-07-28T00:00:00Z \
+  --output-dir artifacts/runs/q0015
 ```
 
 ## Source baseline
 
-The public source baseline is commit `cfadd24b52546d4d5800c4a3c5a75a2add86f928` dated 2026-07-28. See [`docs/BASELINE.md`](docs/BASELINE.md).
+The public source baseline is commit
+`cfadd24b52546d4d5800c4a3c5a75a2add86f928` dated 2026-07-28.
+See [`docs/BASELINE.md`](docs/BASELINE.md).
diff --git aa/SHA256SUMS bb/SHA256SUMS
index 69923ef..e148486 100644
--- aa/SHA256SUMS
+++ bb/SHA256SUMS
@@ -1,29 +1,30 @@
-52f28ebba0d940669fa2ce44c61fd16131f1851b14fcaaac2f246d093c77a2c9  .github/workflows/enumerate-smoke.yml
+ae4a3fae37f05de5f6912d2650665287e2c1dd991f3acf6f5d2f9418fdafec3e  .github/workflows/enumerate-smoke.yml
 9dc032f925ef4809d9fbac48b927c1fb5d715333085719b1f6588b0d2c671eed  .gitignore
 dab3be154d8bc336fc0339ede2d8bd8bcab0052fa42e334075cea7ef5e8c09de  AGENTS.md
-ab2ef34b526c2987040f9e474ada77cb91dfd25d99127f199c1a6d223612a32b  DELIVERY_MANIFEST.json
-ea30ee2d423987e05e195cc1f66066eb0cd1e699e08ee388d8c6e48839670e28  FACTS.md
-a3f0aadaceebe923fa26d9814f2c7981e59075c5c299e098821699808fd9c2b9  FAILURES.md
+3cacec3029a9c48f969b74389ddee383025357d766ffd595a6626fb1e5af1fc4  DELIVERY_MANIFEST.json
+7baa107859b2cfa4e0ef93c99a985c67451279f5aeace100cfe75b5eebb8d7d6  FACTS.md
+eb9819a981eea0e49e859c4cf558f7b408129434138d9f6ff8b7760b804bb948  FAILURES.md
 10a069b4b305bb58c0efd3c440033648ae521d92f7ace14648037423179db450  HANDOFF_CURRENT.md
-778d738fd4f6d6d7266effa932859b836b454db4ebe3c2c76c8b71a6d67e27df  PIVOT_SWITCH_ESCAPE_FRAMEWORK.md
-763babfbbb73781d9dec74276aa0e09155e2bcad8a1091493cd42e5a6c46726a  QUESTIONS.md
-d562f0146a551905d2d204cf363eb7855a0c631a00c644cb3421b0f114646f0a  README.md
-d7678d2beeb3d84681e05b4c3fb27dcd0f338212cda118a18af250e15111acb1  SINGLE_DEFECT_FRAMEWORK.md
+7786d5fe306c043e73bd340487813b4402608bed0c797ba008ef7ba83f1d066b  PIVOT_SWITCH_ESCAPE_FRAMEWORK.md
+a6e0a38bdec0caa46d48871a1d209e78a2370266ace38454870d0e16c811e266  QUESTIONS.md
+56b76d251159bba4ca84f07e129740af5f07225b7217c6a3e048349fa5d0639d  README.md
+5c2393a8618cd09f441b93d580a039b2c8d789d263cfeee0a4eef9a06649e8e1  SINGLE_DEFECT_FRAMEWORK.md
 7aed553ebabee865e5da741a8445a354dc88a710ab3989f94ec6a0249e09f295  UNZIP_README.md
-d7a89ab3407e48a54f68201d6ee629da06ef49d257eafc300aa68fb02f7f2748  WORKFLOW.md
-e6a3c490f56702e3f3033c339f139263b92cb3c2d2448756a6e4c344c00bf7c4  agent.md
+86805c7692119bb7dba43188c4468bcc7448fcd4daea72267deba3fd1ac0b00c  WORKFLOW.md
+19096559e63e9b3a5cb07a01b536a6ace6f0a8eb977200353acb9824e5c308c3  agent.md
 4ba5d835a656529a38ebc7d06093125aeafd38c0cf805c61d412fdafb09dc4e5  docs/BASELINE.md
 1271e94ccaeeeec60fc1c5c1f9d883aae8a262b05be4e7cbcfbf68f5f68ae9d1  docs/BASELINE_MANIFEST.json
 9f64c7369084ba0edf55357c443a10698effa1c1bbcf10734bdfe7728479cc10  docs/CLAIM_STATUS_POLICY.md
 d522587f368591266b3b95d4e64ef74d8419b8adf328437deee26a0ac2921ec2  docs/KNOWN_LIMITATIONS.md
 ef74d9800534be3a5cdbfc778beaa8bfb4cc842f9979c213e07741d909fd5651  docs/MIGRATION_V3.md
 250db202332d69979ce7cf0d6dc505cbc861684b82390b41504fb7c8b5dfff5b  docs/NOTATION_AND_TYPES.md
-93db25cea81ba42cda2d573d3f912e23abd4f9c5e0b967522c22a4bfc92e6bca  docs/PROJECT_STATE.yaml
+bd62d5b4b735215d6aff224c5f10ea5dd741492b27212ef9b164b094b78ffdae  docs/PROJECT_STATE.yaml
 cc27c7beac09af8ec08a35deb67bfee1a759923dded68361bc357cf6710f74a7  docs/PROJECT_WORKFLOW.md
 bedf035fcf48a0d24347af193d873ff4ff4412eea79b7e25b6a7453af09d6f4f  docs/PROOF_DAG.md
 4fa6e7e7a82efd2b949ab2f5a0eb10b02131bae0980d88fbdf4213352e64d9ef  docs/QUICKSTART_10_MINUTES.md
 fae922be6948977095129cb44ae669afbc1a3fe1205efab304708a477a31a9bf  docs/README.md
 0e0bd7a87615a29288b12a21e26eacffb2c24c1a0526681bd4921192398d6d24  docs/STATUS_CARD.md
+6acfe2f9395f11aef4ddd319d7e29cb63e07eef417886a5a200294d75b452ef9  docs/changes/2026-07-28-acceptance-fix.md
 9aed1ec7a687e2c66b392d95874390483573d5b5953b9088be4debcad0dd51d7  docs/changes/2026-07-28-general-workflow-restructure.md
 3e7687833e84556d59b354cc0e0c3985d1c70b5fdf8e86bb37713a195229088f  docs/changes/README.md
 8de5c2f88749179bbfcf47958366abe107524f10402bdf0a03a1465340e22349  docs/framework/FW-10_CONFIGURATION_ENTRY.md
@@ -41,7 +42,7 @@ aab420e6944f73c777a6a6f4edbbcbd74f1fc1868fbaef0eb44937c8ddeefd6c  docs/framework
 d5ce8417c1d58edccaa30d78020be3bb56a136bdca09744f9e8e524413514b85  enumerate/schemas/e_exit.schema.json
 e739738c16a395d2887b7b91ad6e1831ce81ad74ac8f390ed89912dd656c5223  evidence/README.md
 afafde347b897c7e35061ad65a3f714b3176ee5b2f253e5a1c65562ff1b26618  evidence/audits/REPOSITORY_AUDIT.md
-b5aa391decaea21ebb0c75288d1b1c5a9561a74c382c94c06a0dd3fe8346a31f  evidence/audits/VALIDATION.md
+b47e301e8ee9018c1bb438f43bc5bfa86c4f9fae1f3f20f4eb15d796531cb219  evidence/audits/VALIDATION.md
 5d74baf30661f3f9add761cbe276eeb8152aabb5463176858a944c0ecbd8cae5  evidence/experiments/q0015/MANIFEST.json
 c5eca00a74ee8103e3fc2dd6645f46b7f048bad3b06a6d5609e69c1610ade0a5  evidence/experiments/q0015/baselines/q0015_audit_results.json
 599a18103beef8a95fcf365d87d2df96b98bff28b5d8c6f0195611321e5a2d2f  evidence/experiments/q0015/historical/q0015_audit_results.pre-envelope.json
@@ -49,7 +50,7 @@ ca370ecc15fbb4cecb7af28f39ee5ee7fe5f3acf7e62c74d52721728a87cae20  evidence/exper
 80f27758ecc8f2b1b015182be864b19c647bf38e2257b44887cc2e38bfe0c139  evidence/experiments/q0015/reports/q0015_first_execution_report.md
 8034a5f0452df38fc0d95e97085b5f7836537eddbcef15ccac0e8dd09d8ef7b9  evidence/experiments/q0015/reports/q0015_hall_cut_structural_analysis.json
 883db9da10f22437c34ec17ffa8fa0454b5f73ac65b565baf9e493697e86f72f  evidence/experiments/q0015/reports/q0015_hall_cut_structural_analysis.md
-ba536762d7decc44c64362f9c6dc668a0a9d222e88ee3e94a723cb4363ef2392  history/CHANGELOG.md
+eea99b5b3085084b9dba305953618df35871fa6cdcf65c2c57e951d423f5c05a  history/CHANGELOG.md
 9d46f128ed9ae6b287576d2504a56358a6af8147449299f964fe7f75b50340cf  history/README.md
 192b2139e51e1d9089f0bed6e88ceb07fee8b19f54f62921e153ad76e2432c03  history/legacy-frameworks/pivot-switch-escape-monolith.md
 fda12b7a13c401b1b53aeb0abce25698811a364ff68a7101c1c1eef0ff411063  history/legacy-frameworks/single-defect-monolith.md
@@ -60,7 +61,7 @@ d6c54e0e9498ec1cab27c89a2173a92ef132241b95feda9fec604aab5480a05d  knowledge/FACT
 e9368dc36c9adfb4520d2777ef5a5aa6c2405fbab52e09e6eb97667b0ac5b7cd  knowledge/QUESTIONS.md
 dc3ad9347851c468d740f1f9d5691d54bc6ef2493e2dec6163b8956d6c72c55f  knowledge/README.md
 f171b950465dc9c7653d538fab69e2a0e202ddc0f3a32112ea926e7b2c0429f7  old/README.md
-f046cc4fc4cd1c39d3d50fe817b191cc1e0ee971d2a41cc4ad4f835973c0e10c  pyproject.toml
+74f77bafd0f2326f5b6d535987eae235db25f0508e6d0cab0e9fcb26337ee729  pyproject.toml
 2392c8a827e9b196a3ff1d5af4ef211de580f1264de20641f19bc63fdfc0fbf5  requirements.txt
 9614e9b142ad09a801e1bf4b60bb1f0d5fe127da7113ca6fe91b00445d8ce79b  sources/raw/MANIFEST.json
 98a34b24ab32a2729457eafc317fa0c74c13e5cc8c8685020c280934e4082e4c  sources/raw/README.md
@@ -87,6 +88,6 @@ caa3a6a94cd52b7ede852df1f6e37ec3bd66a93dc80763d42355e13e3e5af7b9  tests/test_cer
 73db495c70de5b6d6ae1dbd193520c6023617c85636bb4f430c6a782989fc1a7  tests/test_ledgers.py
 f0d122f9bc93bc98fec8fc433bacf22439b027604e21d17b0af38a687505485c  tests/test_q0015_regressions.py
 befa38735506fb6d7e1c403ae10ed9dad26c08317d74b0e0de5b114de85483aa  tools/check_generated_artifacts.py
-dde0b68573c1b232649734390f67c7a2d6b0592cc23ea43fe97338127a58ba69  tools/check_repository.py
+3939d076d5eef0f14bcc3f1a40f897dcd54fef5590c816badc9b5ba63bd867bc  tools/check_repository.py
 f0335369d30380362ba973bb1c6b677ce41d71ccd4490ca3fec310f41c9ceca1  tools/fetch_upstream_archives.py
 ea215a790d3326d33822345e4282d393438950de0902307885e4ff723db2dec1  tools/regenerate_baseline.py
diff --git ab/SINGLE_DEFECT_FRAMEWORK.md bb/SINGLE_DEFECT_FRAMEWORK.md
new file mode 100644
index 0000000..342b991
--- /dev/null
+++ bb/SINGLE_DEFECT_FRAMEWORK.md
@@ -0,0 +1,5 @@
+# Compatibility pointer
+
+Historical monolith: [`history/legacy-frameworks/single-defect-monolith.md`](history/legacy-frameworks/single-defect-monolith.md).
+
+Current modules: [`docs/framework/`](docs/framework/).
diff --git aa/WORKFLOW.md bb/WORKFLOW.md
index 906ee8b..9442c5f 100644
--- aa/WORKFLOW.md
+++ bb/WORKFLOW.md
@@ -1,427 +1,200 @@
-# 对话驱动的研究与工程仓库工作流
+# 对话驱动项目工作流
 
-## 1. 目的
+本文件解释 [`agent.md`](agent.md) 中的强制规则，并提供可复用模板。
+`agent.md` 是短协议；本文件是按需读取的操作手册。项目专有规则仍以
+[`AGENTS.md`](AGENTS.md) 为准。
 
-本工作流适用于由人类与 AI 通过多轮对话共同推进的研究、软件、分析、设计和知识管理项目。
+## 1. 为什么要分层
 
-目标是把对话中的有效成果转化为：
+长期对话项目最常见的问题不是缺少文字，而是以下内容混在一起：
 
-- 可复用的项目知识；
-- 可检查的证据或实现；
-- 明确的开放问题；
-- 干净的当前状态；
-- 可追踪但不干扰当前工作的历史。
+- 原始输入；
+- 当前可信知识；
+- 完整证据；
+- 临时探索；
+- 当前行动；
+- 历史修订。
 
-核心原则是：
+推荐分为五层：
 
-> 对话用于探索，仓库用于确立状态；  
-> 注册表说明“可以依赖什么”，证据文件说明“为什么可以依赖”，当前交接说明“下一步做什么”。
-
----
-
-## 2. 信息分层
-
-项目内容应至少分为五层。
-
-### 2.1 原始来源层
-
-保存未经改写的输入，例如：
-
-- 对话导出；
-- 原始需求；
-- 外部资料；
-- 数据快照；
-- 用户提供的文件；
-- 会议记录。
-
-原始来源通常只读。发现错误时，应在审计或失败记录中说明，不应回写原文以“修正历史”。
-
-### 2.2 知识注册层
-
-保存经过整理、具有稳定身份的项目条目，例如：
-
-- 已确认事实；
-- 需求与约束；
-- 决策；
-- 已知失败；
-- 开放问题；
-- 接口和定义。
-
-注册条目应包含精确陈述、适用范围、状态、依赖、证据位置和限制，但不应容纳全部推导、日志或实现细节。
-
-### 2.3 证据与实现层
-
-保存注册条目成立的依据，例如：
-
-- 完整证明或分析；
-- 源代码；
-- 测试；
-- 实验；
-- 数据处理脚本；
-- 审计报告；
-- 机器证书；
-- 设计说明。
-
-注册表类似接口，证据与实现类似接口背后的实现。
-
-### 2.4 当前状态层
-
-保存下一位参与者立即需要的信息，例如：
-
-- 当前目标；
-- 已确认输入；
-- 当前阻塞；
-- 最高优先级问题；
-- 下一步动作；
-- 必读文件。
-
-`HANDOFF_CURRENT.md`、`CURRENT_STATE.md` 一类文件属于当前状态视图，不是历史日志，也不是权威证据来源。
-
-### 2.5 历史层
-
-历史主要由以下方式保存：
-
-- Git 提交记录；
-- 独立审计文件；
-- 变更日志；
-- 归档快照；
-- 原始对话。
-
-不应通过在所有当前文件尾部持续追加“最新补充”来保存历史。
-
----
+| 层 | 作用 | 默认更新语义 |
+|---|---|---|
+| 原始来源 | 保存上下文和输入 | 只读 |
+| 知识注册 | 说明当前可依赖什么 | 原地更新 |
+| 证据与实现 | 说明为什么可信 | 原地整理或新建独立记录 |
+| 当前状态 | 指引下一步 | 重写当前快照 |
+| 历史 | 保存演化过程 | Git、归档、日志 |
 
-## 3. 推荐目录
+一个条目出现在注册表中，不代表它已经被证明；其状态和证据链接共同决定可信程度。
 
-项目可按实际规模调整，但建议保持以下职责分离：
+## 2. 推荐目录
 
 ```text
 project/
 ├── README.md
 ├── AGENTS.md
+├── agent.md
 ├── WORKFLOW.md
 ├── HANDOFF_CURRENT.md
-│
 ├── knowledge/
 │   ├── FACTS.md
 │   ├── REQUIREMENTS.md
 │   ├── DECISIONS.md
 │   ├── FAILURES.md
 │   └── QUESTIONS.md
-│
 ├── evidence/
 │   ├── proofs/
 │   ├── analyses/
 │   ├── audits/
 │   ├── experiments/
 │   └── certificates/
-│
 ├── src/
 ├── tests/
 ├── tools/
-│
-├── sources/
-│   └── raw/
-│
+├── sources/raw/
 └── history/
 ```
 
-较小项目可以使用集中式文件；较大项目可以将每个条目拆成独立文件，并让顶层文件只做索引。
-
----
+小项目可以使用集中式文件；大项目可以每条记录一个文件，并让顶层文件只做索引。
 
-## 4. 文件更新语义
+## 3. 一轮工作的生命周期
 
-不同文件不应采用同一种更新方式。
+### 3.1 读取
 
-| 文件类型 | 推荐更新方式 | 说明 |
-|---|---|---|
-| 事实、需求、问题、决策 | 结构化原地更新 | 同一条目保持稳定 ID 和唯一当前版本 |
-| 证明、分析、实现说明 | 原地整理，Git 保存旧版本 | 最终文件应连续可读，不应堆叠修补段落 |
-| 失败记录 | 新增独立失败，已有失败原地补充 | 重点保存失败机制与重试条件 |
-| 审计报告 | 通常新建 | 每次审计是独立事件 |
-| 实验日志 | 新建或按运行分目录 | 必须可复现 |
-| CHANGELOG | 追加 | 本身就是时间序列 |
-| HANDOFF_CURRENT | 重写为当前快照 | 删除过时目标、旧阻塞和已完成动作 |
-| 原始来源 | 只读 | 错误通过旁路记录说明 |
+依次读取项目规则、README、当前 handoff、相关知识与失败、证据和必要来源。
+不要把聊天摘要当成仓库状态。
 
-### 4.1 当前交接文件的特殊规则
+### 3.2 范围锁定
 
-`HANDOFF_CURRENT.md` 必须：
-
-1. 在修改前完整读取；
-2. 与权威知识、证据和问题记录核对；
-3. 原地替换过时内容；
-4. 删除已经解决的问题和失效动作；
-5. 合并重复表述；
-6. 保持短小；
-7. 读起来像在当前时刻一次写成；
-8. 不追加连续的日期更新区块。
-
-Git 已经承担历史审计，不需要让当前交接文件兼任 changelog。
-
----
-
-## 5. 条目状态
-
-项目应采用有限、明确的状态集合。推荐：
-
-- `verified`：已有充分、可检查的证据；
-- `verified-conditional`：只在明确附加条件下成立；
-- `implemented`：功能已实现并通过规定测试；
-- `observed`：仅有有限实验或样例支持；
-- `proposed`：候选方案或待验证陈述；
-- `partially-verified`：只完成部分范围；
-- `open`：尚未解决；
-- `blocked`：缺少必要输入、工具或依赖；
-- `disputed`：来源或审计结果冲突；
-- `refuted`：已有有效反例或否定证据；
-- `superseded`：被更准确的版本替代；
-- `deprecated`：仍存在但不应继续使用。
-
-状态必须反映证据强度，而不是对话语气或参与者信心。
-
-以下内容不能单独支持 `verified`：
-
-- AI 在对话中的肯定回答；
-- 用户未经验证的断言；
-- 少量样例；
-- 随机测试；
-- 超时；
-- “没有发现问题”；
-- 未检查适用条件的外部引用；
-- 只在新定义的理想接口中成立的结果。
-
----
-
-## 6. 一轮工作的完整生命周期
-
-## 6.1 启动与读取
-
-开始前依次读取：
-
-1. 项目规则；
-2. 当前交接；
-3. 与任务有关的知识条目；
-4. 相关失败记录；
-5. 相关证据、实现和测试；
-6. 必要的原始来源。
-
-不得仅凭对话摘要或上一次会话记忆继续工作。
-
-## 6.2 写前范围锁定
-
-在第一次修改前，记录：
+使用：
 
 ```text
-任务：
-工作模式：
-当前基础版本：
-目标：
-输入：
-输出：
-允许修改的路径：
-不得修改的路径：
-预期状态变化：
-验收标准：
-必须运行的检查：
-明确不解决的内容：
+Task:
+Mode:
+Base version:
+Target:
+Inputs:
+Outputs:
+Allowed paths:
+Forbidden paths:
+Expected status change:
+Acceptance criteria:
+Required checks:
+Non-goals:
 ```
 
-工作模式至少区分：
+范围锁定的目的不是增加仪式，而是防止任务从一个小修复扩张成全仓库重写。
 
-- `read-only`：只分析；
-- `proposal`：只生成方案或补丁；
-- `implementation`：修改实现、测试或文档；
-- `status-promotion`：允许改变项目权威状态。
+### 3.3 提取变化
 
-实现任务不自动获得状态提升权限。
-
-## 6.3 提取本轮变化
-
-从对话或工作结果中提取候选变化，并分类为：
+从对话和工作结果中提取：
 
 ```text
-ADD        新增
-UPDATE     修正或扩展
-CONFIRM    补充可靠证据
-DOWNGRADE  降低可信状态
-REFUTE     否定
-SUPERSEDE  用新版本替代
-MERGE      合并重复条目
+ADD        新增独立内容
+UPDATE     修正或扩展已有内容
+CONFIRM    增加可靠证据
+DOWNGRADE  降低状态
+REFUTE     否定陈述或方法
+SUPERSEDE  用更准确版本替代
+MERGE      合并重复内容
 CLOSE      关闭问题
-REOPEN     重新打开
-NO_CHANGE  不应写入仓库
+REOPEN     重新打开问题
+NO_CHANGE  不应进入仓库
 ```
 
-每项变化必须说明：
-
-- 影响哪个已有条目；
-- 依据是什么；
-- 应修改什么文件；
-- 是否需要人工复核。
+变化清单应写明依据、目标文件、状态影响和人工复核需求。
 
-## 6.4 语义查重
+### 3.4 语义查重
 
-新增条目前必须检查它是否：
+检查候选内容是否已经存在、只是换一种措辞、属于特例或推广、与现有记录冲突，
+或重复已知失败。重点比较前提、范围、结论和机制，而不是关键词。
 
-- 已经存在；
-- 只是已有内容换一种说法；
-- 是已有条目的特例；
-- 是已有条目的推广；
-- 修正了已有范围；
-- 与已有内容冲突；
-- 重复了已记录失败；
-- 已在某个开放问题中出现。
+### 3.5 建立证据
 
-查重应比较对象、前提、范围、结论、机制和证据，而不只是关键词。
+优先生成能失败的测试、验收用例或反例，再修改实现或正文。
+完成后进行敌对审计：
 
-## 6.5 执行工作
+- 是否遗漏前提；
+- 是否扩大范围；
+- 是否混淆局部和一般结论；
+- 是否存在循环依赖；
+- 是否忽略边界；
+- 是否把超时或工具错误当成对象错误；
+- 是否重复旧失败。
 
-工作过程中应优先生成：
+### 3.6 写回
 
-1. 可失败的检查或测试；
-2. 实现、分析或证明；
-3. 机器可读输出；
-4. 审计结果；
-5. 状态更新。
+先写证据，再写知识，最后写状态和 handoff。
+修改当前条目时应原地整理，不要堆叠修正说明。
 
-不得先修改“已完成”状态，再寻找支持它的证据。
+### 3.7 验证和交付
 
-## 6.6 敌对审计
+运行实际存在的检查，审阅完整 diff，确认没有无关修改，再生成完成报告。
+默认不提交、不推送。
 
-在宣布完成前，主动检查：
+## 4. 文件更新决策
 
-- 是否引入了未声明条件；
-- 是否错误扩大适用范围；
-- 是否混淆样例和一般结论；
-- 是否循环依赖；
-- 是否忽略边界情况；
-- 是否把工具失败当成对象失败；
-- 是否修改了结论以迁就实现；
-- 是否存在更小反例；
-- 是否有旧失败路线被换名重复；
-- 是否有证据文件和注册状态不一致。
-
----
-
-## 7. 事务式写入顺序
-
-推荐采用以下顺序：
-
-```text
-测试、反例或验收用例
-        ↓
-实现、分析或证明正文
-        ↓
-机器输出、日志和证书
-        ↓
-事实、失败、问题和决策
-        ↓
-主题文档或设计文档
-        ↓
-项目状态图或机器状态
-        ↓
-HANDOFF_CURRENT
-        ↓
-变更日志与完成报告
-```
+### 新建文件
 
-关键原则：
+适用于：
 
-> 证据先写，状态后写；当前交接最后写。
-
-若证据层验证失败：
-
-- 不得提升状态；
-- 不得关闭问题；
-- 不得把交接改成“已完成”；
-- 应保留最后一个已验证状态；
-- 将失败记录为实现或审计问题。
-
----
-
-## 8. 补丁与原地修改
-
-修改已有文件时：
-
-1. 先读取完整相关部分；
-2. 找到旧内容应被替换的位置；
-3. 优先进行最小语义补丁；
-4. 删除被新内容取代的旧表述；
-5. 保留稳定 ID 和外部接口；
-6. 查看完整 diff；
-7. 避免无关格式化。
-
-不得默认在文件尾部追加修正说明。
-
-以下情况适合新增文件：
-
-- 新的独立事实或失败；
-- 新的一次审计；
-- 新的一次实验；
-- 新的证明或分析模块；
+- 新的独立事实、问题或失败；
+- 新的一次审计或实验；
+- 新的证明、分析或设计模块；
 - 新的历史快照。
 
-以下情况适合原地修改：
+### 原地修改
+
+适用于：
 
 - 同一条目的状态变化；
-- 陈述范围修正；
-- 当前证明的漏洞修复；
-- 当前交接更新；
+- 范围修正；
+- 当前证明或实现的漏洞修复；
 - 过时下一步的替换；
+- 当前 handoff；
 - 重复内容清理。
 
----
-
-## 9. 机器输出和生成文件
-
-所有生成结果应记录：
-
-- 生成脚本；
-- 精确命令；
-- 输入；
-- 参数；
-- 随机种子；
-- 工具和依赖版本；
-- 基础提交或数据版本；
-- 运行时间；
-- 结果类型；
-- SHA-256。
+### 追加
 
-结果类型应明确区分：
+主要适用于：
 
-- exhaustive；
-- bounded exhaustive；
-- random；
-- approximate；
-- timeout；
-- interrupted；
-- infeasible with certificate；
-- unresolved。
+- changelog；
+- 时间序列日志；
+- 独立运行索引。
 
-生成文件应采用原子更新：
+“可审计”不意味着所有文件只能追加；Git 已经保存历史。
 
-1. 写临时文件；
-2. 验证格式和内容；
-3. 验证成功后替换目标；
-4. 验证失败时保留旧文件。
+## 5. 状态建议
 
-不得手工编辑机器结果来使测试通过。
+若项目没有自己的体系，可以使用：
 
----
+| 状态 | 含义 |
+|---|---|
+| `verified` | 有完整可检查证据 |
+| `verified-conditional` | 只在明确附加条件下成立 |
+| `implemented` | 已实现并通过规定测试 |
+| `observed` | 仅有有限实验或样例 |
+| `proposed` | 候选方案 |
+| `partially-verified` | 只完成部分范围 |
+| `open` | 尚未解决 |
+| `blocked` | 缺少依赖、输入或工具 |
+| `disputed` | 来源或审计冲突 |
+| `refuted` | 有有效否定证据 |
+| `superseded` | 被更准确版本替代 |
+| `deprecated` | 保留但不应继续使用 |
 
-## 10. 事实与证据的连接
+AI 的肯定回答、少量样例、随机测试、超时和“没有发现问题”均不能单独支持
+`verified`。
 
-知识条目建议包含：
+## 6. 知识条目模板
 
 ```markdown
-# 条目标题
+# 标题
 
 - **ID:** K-xxxx
 - **Status:** verified
 - **Statement:** 精确陈述
-- **Scope:** 适用范围
+- **Scope:** 适用条件和边界
 - **Dependencies:** K-xxxx, K-yyyy
 - **Evidence:** `evidence/...`
 - **Mechanism:** 简短说明为何成立
@@ -429,28 +202,14 @@ HANDOFF_CURRENT
 - **Updated:** YYYY-MM-DD
 ```
 
-完整论证、实现或实验放在独立文件中。
+完整证明、实现、分析或实验放在独立证据文件中。
 
-最终使用这些知识构建更大结论时，不能只机械列出 ID；必须逐个检查：
-
-- 当前对象是否满足前提；
-- 量词是否兼容；
-- 范围是否一致；
-- 结论是否足够强；
-- 依赖是否已验证；
-- 模块之间是否缺少连接步骤。
-
----
-
-## 11. 开放问题与失败记录
-
-### 11.1 开放问题
-
-一个合格问题必须包含：
+## 7. 开放问题模板
 
 ```markdown
 # 问题标题
 
+- **ID:** Q-xxxx
 - **Status:** open
 - **Question:** 可独立回答的问题
 - **Why it matters:** 阻塞什么
@@ -460,250 +219,188 @@ HANDOFF_CURRENT
 - **Next action:** 一个具体动作
 ```
 
-问题解决后应原地改为关闭状态并指向证据，不应删除。
-
-### 11.2 失败记录
+问题解决后原地更新并链接证据，不要删除历史 ID。
 
-失败记录必须说明机制，而不仅是“没有成功”：
+## 8. 失败记录模板
 
 ```markdown
 # 方法标题
 
+- **ID:** A-xxxx
 - **Goal:** 原目标
 - **Approach:** 核心方法
 - **Failure type:** logical / counterexample / timeout / unsupported assumption / ...
-- **Failure point:** 最具体的断点
+- **Failure point:** 最具体断点
 - **Why it failed:** 已确认原因
-- **Failure signature:** 用于查重的标签
+- **Failure signature:** 用于识别换名重复
 - **Evidence:** 证据位置
 - **Retry conditions:** 何时值得重试
 - **Do not repeat:** 哪些表面变化仍属同一路线
 ```
 
-超时、工具报错和暂时没做完，不应自动登记为数学或方案上的失败。
+超时、工具报错和暂时未完成不应自动登记为原理失败。
+
+## 9. 机器产物模板
+
+每个生成产物至少记录：
+
+```yaml
+generator: tools/example.py
+command: python tools/example.py --input ...
+inputs:
+  - data/input.json
+parameters:
+  seed: 1234
+dependencies:
+  python: "3.12"
+base_version: "<commit-or-data-version>"
+result_type: exhaustive
+generated_at: "YYYY-MM-DDTHH:MM:SSZ"
+payload_sha256: "<sha256>"
+```
 
----
+应先写临时文件，完成 schema 和哈希验证后再原子替换目标。
 
-## 12. 当前交接模板
+## 10. 当前 handoff 模板
 
 ```markdown
 # Current Handoff
 
-## 1. Objective
+## Objective
 
-当前项目目标和成功标准。
+当前目标与成功标准。
 
-## 2. Current state
+## Current state
 
 - 当前阶段：
 - 最近完成：
 - 当前阻塞：
-- 当前采用的路线：
+- 当前路线：
 
-## 3. Reliable inputs
+## Reliable inputs
 
-只列下一轮可以安全依赖的条目和证据。
+下一轮可安全依赖的条目和证据。
 
-## 4. Provisional findings
+## Provisional findings
 
-列出尚未完全验证的发现及其状态。
+尚未完全验证的发现及状态。
 
-## 5. Do not repeat
+## Do not repeat
 
-列出相关失败记录及重试条件。
+相关失败及重试条件。
 
-## 6. Open questions
+## Open questions
 
 按优先级列出少量问题。
 
-## 7. Immediate next actions
+## Immediate next actions
 
 最多 3–5 项，每项有明确完成标准。
 
-## 8. Required reading
+## Required reading
 
-列出下一位参与者开始前必须读取的文件。
+下一位参与者开始前必须读取的文件。
 
-## 9. Integrity warnings
+## Integrity warnings
 
-列出冲突、未验证步骤、数据缺口和环境限制。
+冲突、未验证步骤、数据缺口和环境限制。
 ```
 
-每轮更新时，应重新整理整个文件，使其反映唯一的当前状态。
-
----
-
-## 13. 通用流程与项目规则的分离
-
-可复用流程与项目专有状态应分开管理。
+更新后应像当前时刻一次写成，不应保留连续的日期更新区块。
 
-### 13.1 共享工作流
+## 11. 主结论组装检查
 
-适合放在共享仓库、模板或 skill 中：
+使用多个知识模块构建更大结论时，对每个调用点检查：
 
-- 对话知识提取；
-- 事实审计；
-- 证明或分析补全；
-- 代码修改流程；
-- 仓库写回；
-- handoff 更新；
-- diff 和验证检查。
-
-### 13.2 项目本地规则
-
-适合放在项目自己的 `AGENTS.md` 或协议文件中：
-
-- 项目目标；
-- 权威文件路径；
-- 术语和定义；
-- 状态集合；
-- 特定测试命令；
-- 禁止修改目录；
-- 项目独有证书格式；
-- 当前活动目标。
-
-简单判断标准：
-
-> 换一个项目后仍基本原样适用的内容属于共享工作流；  
-> 依赖本仓库对象、脚本、格式或当前状态的内容属于项目规则。
-
-在普通网页对话中，远程工作流文件通常需要被明确指定和读取；不能假设系统会自动发现、安装或持续记住它。
-
----
-
-## 14. Git 与安全规则
-
-除非用户明确要求，自动化参与者不得：
+- 当前对象是否满足前提；
+- 输入输出是否匹配；
+- 范围和量词是否兼容；
+- 依赖是否已验证；
+- 结论是否足够强；
+- 模块之间是否缺少连接步骤；
+- 是否引用被否定或废弃条目。
 
-- 自动提交；
-- 自动推送；
-- 创建或合并 Pull Request；
-- 强制重置工作树；
-- 清理未知文件；
-- 覆盖无关修改；
-- 批量改名或全仓库格式化；
-- 修改原始来源；
-- 将未验证内容写入权威状态。
+最终正式文本应展开逻辑连接，而不是只列内部 ID。
 
-完成后必须：
+## 12. Git 与冲突处理
 
-1. 查看 `git status --short`；
-2. 审阅相关 diff；
-3. 检查是否存在无关修改；
-4. 展示 `git diff --stat`；
-5. 说明运行了哪些检查；
-6. 明确哪些问题仍未解决。
+默认不提交、不推送、不建 PR。不得强制 reset、清理未知文件或覆盖无关修改。
 
-只读环境中，应生成可应用补丁，而不是声称已经修改仓库。
+若当前对话与仓库冲突：
 
----
+1. 不静默覆盖；
+2. 保留双方来源；
+3. 标记冲突或创建问题；
+4. 只有证据足够时才裁决；
+5. handoff 与权威记录冲突时修复 handoff。
 
-## 15. 完整性检查
+## 13. 完整性检查
 
-结束前逐项检查：
+结束前检查：
 
-1. 是否读取了项目规则和当前状态；
-2. 是否限制了修改范围；
-3. 是否查重；
+1. 是否读取项目规则和当前状态；
+2. 是否限制修改范围；
+3. 是否完成语义查重；
 4. 是否保留稳定 ID；
-5. 是否把观察误写成验证结论；
-6. 是否把条件结果误写成一般结果；
-7. 是否存在开放义务却关闭问题；
-8. 是否存在循环依赖；
-9. 是否引用不存在或已废弃的条目；
-10. 是否修改了原始来源；
-11. 是否手改生成结果；
-12. 是否运行了实际存在的检查；
-13. 是否审阅完整 diff；
-14. 是否删除了当前交接中的过时内容；
-15. 是否避免把 handoff 写成 changelog；
-16. 是否诚实记录环境和工具限制；
-17. 是否明确列出仍然开放的问题。
-
----
-
-## 16. 完成报告
-
-每轮完成报告建议使用：
+5. 是否把观察或条件结果写成一般结论；
+6. 是否存在开放义务却关闭问题；
+7. 是否存在循环依赖；
+8. 是否引用不存在或废弃条目；
+9. 是否修改原始来源；
+10. 是否手改生成结果；
+11. 是否运行实际存在的检查；
+12. 是否审阅完整 diff；
+13. handoff 是否删除过时内容并保持简短；
+14. 是否诚实记录环境和工具限制。
+
+## 14. 完成报告模板
 
 ```text
-基础版本：
-工作模式：
-处理范围：
-读取的权威文件：
-
-新增：
-更新：
-降级或否定：
-关闭或重开的问题：
-
-修改文件：
-生成产物：
-运行的检查：
-检查结果：
-
-状态变化：
-未解决问题：
-明确不包含的结论：
-
-Git diff --stat：
-是否提交：否
-建议下一步：
+Base version:
+Mode:
+Scope:
+Authoritative files read:
+
+Added:
+Updated:
+Downgraded or refuted:
+Closed or reopened:
+
+Changed files:
+Generated artifacts:
+Checks run:
+Results:
+
+Canonical status change:
+Unresolved issues:
+Non-goals:
+
+Diff stat:
+Committed: no
+Next action:
 ```
 
-若没有改变项目权威状态，应明确写：
+若无权威状态变化，写：
 
 ```text
 No canonical status was promoted.
 ```
 
----
-
-## 17. 最终原则
+## 15. 总结
 
-整个工作流可以概括为：
+推荐工作链：
 
-\[
-	ext{对话探索}
-
-ightarrow
-	ext{候选变化}
-
-ightarrow
-	ext{语义查重}
-
-ightarrow
-	ext{证据或实现}
-
-ightarrow
-	ext{敌对审计}
-
-ightarrow
-	ext{结构化写回}
-
-ightarrow
-	ext{状态同步}
-
-ightarrow
-	ext{干净交接}.
-\]
-
-可审计性不要求所有文件都只追加。
-
-更合理的分工是：
-
-- Git 保存历史；
-- 原始来源保存上下文；
-- 注册表保存当前可依赖知识；
-- 证据文件保存完整依据；
-- 失败记录保存不可重复的陷阱；
-- 当前交接保存下一步行动。
-
-最终仓库应让新参与者迅速判断：
+```text
+读取权威状态
+→ 锁定范围
+→ 提取候选变化
+→ 语义查重
+→ 建立证据或实现
+→ 敌对审计
+→ 原地整理知识
+→ 最后同步状态
+→ 生成干净 handoff
+→ 审阅 diff
+```
 
-- 当前目标是什么；
-- 哪些内容可以依赖；
-- 这些内容为什么可信；
-- 哪些路线已经失败；
-- 哪些问题仍然开放；
-- 下一步具体做什么。
+强制规则见 [`agent.md`](agent.md)。
diff --git aa/agent.md bb/agent.md
index 16e6d94..f197e3d 100644
--- aa/agent.md
+++ bb/agent.md
@@ -1,857 +1,256 @@
-# AGENT.md — 对话驱动项目的通用代理协议
+# AGENT.md — 通用仓库写入协议
 
-本文件规定 AI、自动化代理和人类辅助工具在本仓库中的工作方式。
+本文件是 AI、自动化代理和人类辅助工具修改本仓库时必须遵守的最小强制协议。
+详细解释、模板和示例见 [`WORKFLOW.md`](WORKFLOW.md)；项目专有规则见
+[`AGENTS.md`](AGENTS.md)。
 
-它适用于研究、软件、分析、设计、知识管理和长期协作项目。  
-项目专有目标、术语、文件路径、测试命令和限制，应写在本仓库自己的 `AGENTS.md`、
-`README.md`、`PROJECT_STATE.md` 或其他项目协议中。
+## 1. 优先级与权威来源
 
-本文件重点约束：
+必须按以下顺序处理冲突：
 
-- 如何读取项目状态；
-- 如何把对话结果写回仓库；
-- 如何区分事实、证据、实现、历史与当前状态；
-- 如何修改已有文件；
-- 如何提升条目状态；
-- 如何验证和交付修改。
+1. 用户当前明确指令；
+2. 当前目录适用的项目规则；
+3. 本文件；
+4. 当前权威知识、证据和状态文件；
+5. 历史资料、旧对话和实验草稿。
 
----
+对话、AI 回答、用户猜测、有限样例和未审计日志只能产生候选变化，不能自动成为项目事实。
 
-## 1. 规范词
+必须区分：
 
-本文使用：
+- 原始来源：默认只读；
+- 知识注册：保存当前可依赖的陈述及范围；
+- 证据与实现：保存证明、代码、测试、分析和证书；
+- 当前状态：保存目标、阻塞和下一步；
+- 历史：由 Git、归档、审计和日志保存。
 
-- **MUST / 必须**：违反即视为无效工作；
-- **MUST NOT / 不得**：禁止行为；
-- **SHOULD / 应当**：除非有明确理由，否则应遵循；
-- **MAY / 可以**：允许但非必需。
+## 2. 工作模式
 
-项目本地规则可以增加限制，但不得降低本文件对证据、状态和写入安全的要求。
+每次任务必须声明一种模式：
 
----
-
-## 2. 权威来源
-
-代理必须明确区分以下来源。
-
-### 2.1 权威项目状态
-
-通常包括：
-
-- 项目协议；
-- 当前状态文件；
-- 事实、需求、决策、问题和失败注册表；
-- 已验证的证明、分析、代码、测试和机器证书；
-- 当前仓库中的正式文档。
-
-### 2.2 非权威来源
-
-通常包括：
-
-- 当前对话中的临时判断；
-- 旧聊天摘要；
-- 未经验证的 AI 回答；
-- 用户的非正式猜测；
-- 过时 handoff；
-- 实验草稿；
-- 未审计日志；
-- 历史对话中的中途结论。
-
-对话可以产生候选变化，但不能自动覆盖仓库中的权威状态。
-
----
-
-## 3. 信息分层
-
-代理必须区分以下五层。
-
-### 3.1 原始来源层
-
-例如：
-
-- 对话导出；
-- 原始需求；
-- 用户上传文件；
-- 外部资料；
-- 数据快照；
-- 会议记录。
-
-原始来源默认只读。  
-发现错误时，应通过审计、失败记录或勘误说明，不得重写原文来消除历史错误。
-
-### 3.2 知识注册层
-
-例如：
-
-- 事实；
-- 需求；
-- 约束；
-- 决策；
-- 失败；
-- 开放问题；
-- 接口；
-- 定义。
-
-注册条目保存：
-
-- 精确陈述；
-- 适用范围；
-- 状态；
-- 依赖；
-- 证据位置；
-- 简短机制；
-- 限制与风险。
-
-注册条目不应包含全部证明、完整日志或大段实现细节。
-
-### 3.3 证据与实现层
-
-例如：
-
-- 完整证明；
-- 分析报告；
-- 源代码；
-- 测试；
-- 实验；
-- 数据处理脚本；
-- 审计；
-- 机器证书；
-- 设计说明。
-
-知识注册表说明“可以依赖什么”；证据与实现说明“为什么可以依赖”。
-
-### 3.4 当前状态层
-
-例如：
-
-- `HANDOFF_CURRENT.md`
-- `CURRENT_STATE.md`
-- 当前任务清单
-- 当前活动节点
-
-当前状态文件只回答：
-
-- 现在在做什么；
-- 当前可靠输入是什么；
-- 最大阻塞是什么；
-- 下一步是什么；
-- 新参与者应先读什么。
-
-它不是完整历史，也不是权威证据来源。
-
-### 3.5 历史层
-
-历史应主要由以下方式保存：
-
-- Git 历史；
-- changelog；
-- 独立审计文件；
-- 原始对话；
-- 归档快照；
-- 实验日志。
-
-不要通过在所有当前文件尾部持续追加“最新补充”来保存历史。
-
----
-
-## 4. 工作模式
-
-每次任务必须属于以下一种模式。
-
-### 4.1 `read-only`
-
-只读取、解释、审计或提出建议。  
-不得修改仓库文件。
-
-### 4.2 `proposal`
-
-可以生成：
-
-- 修改方案；
-- 新文件草稿；
-- 统一 diff；
-- 迁移计划。
-
-不得修改权威状态。
-
-### 4.3 `implementation`
-
-可以修改：
-
-- 源码；
-- 测试；
-- 工具；
-- 文档；
-- schema；
-- 证据文件。
-
-但不得自动提升知识条目或项目状态。
-
-### 4.4 `status-promotion`
-
-可以更新：
-
-- 事实状态；
-- 问题状态；
-- 项目状态图；
-- 当前 handoff；
-- 进展日志。
-
-只有满足本文件的状态提升条件后才允许进入此模式。
+- `read-only`：只读取和审计；
+- `proposal`：只生成方案或补丁；
+- `implementation`：修改实现、测试、证据或文档；
+- `status-promotion`：允许改变权威条目或项目状态。
 
 `implementation` 不自动包含 `status-promotion`。
 
----
-
-## 5. 写前范围锁定
+## 3. 写前范围锁定
 
-在第一次修改前，代理必须记录：
+第一次修改前必须记录：
 
 ```text
-任务：
-工作模式：
-基础版本或提交：
-目标：
-输入：
-输出：
-允许修改的路径：
-不得修改的路径：
-预期状态变化：
-验收标准：
-必须运行的检查：
-明确不解决的内容：
+Task:
+Mode:
+Base version:
+Target:
+Inputs:
+Outputs:
+Allowed paths:
+Forbidden paths:
+Expected status change:
+Acceptance criteria:
+Required checks:
+Non-goals:
 ```
 
-如果无法确定基础提交，必须写明：
-
-```text
-Base version: unavailable
-Reason: ...
-```
+无法确定基础版本时必须如实说明。没有允许路径和验收标准时，不得进行大规模写入。
 
-不得在没有 `允许修改的路径` 和 `验收标准` 的情况下进行大规模写入。
+## 4. 开始前读取
 
----
+按顺序读取：
 
-## 6. 开始工作前必须读取
-
-代理应按以下顺序读取。
-
-1. 当前目录适用的 `AGENTS.md` 或项目协议；
+1. `AGENTS.md` 或当前目录适用的项目规则；
 2. `README.md`；
 3. 当前 handoff 或状态文件；
-4. 与任务直接相关的事实、需求、问题和失败记录；
-5. 对应证据、实现和测试；
+4. 相关知识、失败和问题条目；
+5. 相关证据、实现和测试；
 6. 必要的原始来源。
 
-不得只凭上一轮对话记忆继续工作。
-
-若项目引用共享工作流或外部 skill，必须显式读取所需文件；不得假设它已经自动加载。
+不得仅凭上一轮对话记忆继续工作。共享流程或外部 skill 必须显式读取。
 
----
+## 5. 候选变化与查重
 
-## 7. 对话结果的候选变化
-
-将对话写回仓库前，先提取候选变化：
+写回对话结果前，先分类：
 
 ```text
-ADD        新增条目
-UPDATE     修正或扩展已有条目
-CONFIRM    为已有条目补充可靠证据
-DOWNGRADE  降低可信状态
-REFUTE     否定已有陈述或方法
-SUPERSEDE  用更准确版本替代
-MERGE      合并语义重复条目
-CLOSE      关闭问题
-REOPEN     重新打开问题
-NO_CHANGE  不应写入仓库
+ADD UPDATE CONFIRM DOWNGRADE REFUTE SUPERSEDE MERGE CLOSE REOPEN NO_CHANGE
 ```
 
-每项候选变化必须说明：
-
-- 影响哪个已有条目；
-- 依据是什么；
-- 应修改哪些文件；
-- 是否需要人工复核；
-- 是否会改变项目权威状态。
-
-完成候选变化清单后，才能开始编辑。
+每项必须说明影响对象、依据、目标文件、是否改变权威状态以及是否需要人工复核。
 
----
+新增条目前必须进行语义查重，比较对象、前提、范围、结论、机制和证据；
+不得只比较标题或关键词。
 
-## 8. 语义查重
+## 6. 文件更新语义
 
-新增事实、失败、问题或决策前，必须检查它是否：
+默认规则：
 
-- 已经存在；
-- 只是已有条目的换一种措辞；
-- 是已有条目的特例；
-- 是已有条目的推广；
-- 修正了已有范围；
-- 与已有条目冲突；
-- 重复了已记录失败；
-- 已由已有问题覆盖。
-
-查重必须比较：
-
-- 对象；
-- 前提；
-- 范围；
-- 量词或输入条件；
-- 结论；
-- 核心机制；
-- 证据；
-- 失败原因。
-
-不得只比较关键词或标题。
-
----
-
-## 9. 文件更新语义
-
-不同文件必须采用不同更新方式。
-
-| 文件类型 | 默认更新方式 |
+| 文件类型 | 更新方式 |
 |---|---|
 | 事实、需求、决策、问题 | 原地结构化更新 |
-| 证明、分析、设计说明 | 原地整理，Git 保存旧版本 |
+| 证明、分析、设计说明 | 原地整理；Git 保存旧版本 |
 | 失败记录 | 新失败新增；已有失败原地补充 |
-| 审计报告 | 通常新建 |
-| 实验日志 | 按运行新建 |
+| 审计和实验 | 通常新建独立记录 |
 | CHANGELOG | 追加 |
-| HANDOFF_CURRENT | 重写为当前快照 |
+| 当前 handoff | 重写为当前快照 |
 | 原始来源 | 只读 |
-| 机器生成结果 | 由脚本重新生成 |
-
-### 9.1 不得默认尾部追加
-
-修改已有文件时，代理必须先判断：
+| 机器结果 | 由脚本重新生成 |
 
-- 新内容是在修正旧内容；
-- 替代旧内容；
-- 补充旧内容；
-- 还是一个真正独立的新条目。
+不得用文件尾部的“补充、最新修正、再次修正”代替原地修改。
 
-若是修正或替代，必须原地修改或删除失效内容。  
-不得在文件尾部追加：
-
-```text
-补充：
-进一步补充：
-最新修正：
-再次修正：
-```
+## 7. 证据优先的事务顺序
 
-来回避理解和整理旧内容。
+必须按以下顺序写入：
 
----
-
-## 10. 当前 handoff 的特殊规则
-
-`HANDOFF_CURRENT.md` 是可变的当前状态视图，不是追加日志。
-
-更新时必须：
-
-1. 完整读取当前文件；
-2. 与权威事实、问题、失败和证据核对；
-3. 原地替换过时陈述；
-4. 删除已解决问题；
-5. 删除已经完成或失效的下一步；
-6. 合并重复说明；
-7. 删除不再影响当前工作的历史内容；
-8. 保持短小；
-9. 使文件读起来像在当前时刻一次写成；
-10. 最后再修改该文件。
-
-不得在 handoff 中积累连续的日期更新区块。  
-Git 或 changelog 负责保存历史。
-
----
+1. 测试、验收用例或反例；
+2. 实现、分析或证明；
+3. 机器输出、证书、日志和哈希；
+4. 事实、失败、问题和决策；
+5. 主题或框架文档；
+6. 项目状态；
+7. 当前 handoff；
+8. 进展记录和完成报告。
 
-## 11. 补丁优先
+证据层未通过时，不得提升状态、关闭问题或把 handoff 改成“已完成”。
 
-所有修改应优先采用小型、可审阅补丁。
+## 8. 补丁规则
 
 必须：
 
-- 先读取目标文件相关内容；
-- 找到准确修改位置；
-- 只改完成任务所需范围；
-- 保留稳定 ID 和外部接口；
-- 修改后查看完整 diff；
+- 先读取目标文件相关部分；
+- 优先使用最小语义补丁；
+- 保留稳定 ID 和公共接口；
+- 删除被替代的当前表述；
+- 修改后审阅完整 diff；
 - 对大规模重构提供迁移说明。
 
 不得：
 
-- 未阅读完整相关段落就覆盖文件；
-- 使用宽泛替换修改大量状态或编号；
-- 静默删除仍有引用的内容；
-- 因统一格式而改变含义；
-- 对与任务无关文件进行格式化。
-
----
-
-## 12. 证据优先的写入顺序
-
-一次有效写入应按以下顺序进行。
-
-### Phase A — 证据层
-
-1. 测试、验收用例或反例；
-2. 实现、分析或证明正文；
-3. 机器输出、证书和日志；
-4. 审计结果；
-5. 可复现信息和哈希。
-
-### Phase B — 知识层
-
-6. 事实、需求、失败、问题和决策；
-7. 对应的主题文档或框架文档。
-
-### Phase C — 状态层
-
-8. 项目状态图或机器状态；
-9. 当前 handoff；
-10. 进展日志和完成报告。
-
-不得逆序。
-
-若 Phase A 没有通过验证，Phase C 不得提升状态。
-
----
-
-## 13. 测试与验证
-
-修改实现时，应当先写或同步写测试。
-
-最低要求：
+- 覆盖未完整读取的长文件；
+- 使用宽泛替换批量修改状态或编号；
+- 静默删除仍被引用的内容；
+- 因格式统一改变含义；
+- 修改与任务无关的文件。
 
-- 测试能暴露原问题；
-- 修改后新测试通过；
-- 原有相关回归继续通过；
-- 不只测试正常路径；
-- 对非法输入测试“明确拒绝”；
-- 对错误状态测试不会被误报为成功。
+## 9. 测试、机器结果与验证
 
-如果无法写自动化测试，应提供：
+修改实现时必须新增或同步修改测试，并覆盖失败路径和非法输入。
 
-- 可重复的手工验收步骤；
-- 输入；
-- 预期输出；
-- 实际输出；
-- 环境说明。
+机器结果必须记录生成器、命令、输入、参数、随机种子、依赖版本、基础版本、
+结果类型和 SHA-256。必须先写临时文件，验证成功后再原子替换目标；
+不得手工编辑生成结果使测试通过。
 
----
+常见结果类型必须明确区分：
 
-## 14. 机器输出与生成文件
-
-所有生成结果必须记录：
-
-- 生成脚本；
-- 精确命令；
-- 输入；
-- 参数；
-- 随机种子；
-- 工具与依赖版本；
-- 基础提交或数据版本；
-- 结果类型；
-- SHA-256。
-
-结果类型应明确区分：
-
-- `exhaustive`
-- `bounded`
-- `random`
-- `approximate`
-- `timeout`
-- `interrupted`
-- `unresolved`
-- `certificate-backed`
-
-机器结果必须原子更新：
-
-1. 写临时文件；
-2. 验证格式与内容；
-3. 验证成功后替换目标；
-4. 验证失败时保留旧文件。
-
-不得手工修改机器结果来使测试通过。
-
----
-
-## 15. 状态体系
-
-项目应采用有限、明确的状态集合。若项目没有自己的定义，可使用：
-
-- `verified`
-- `verified-conditional`
-- `implemented`
-- `observed`
-- `proposed`
-- `partially-verified`
-- `open`
-- `blocked`
-- `disputed`
-- `refuted`
-- `superseded`
-- `deprecated`
-
-状态反映证据强度，不反映对话语气、信心或工作量。
-
-以下内容不能单独支持 `verified`：
-
-- AI 的肯定回答；
-- 用户未经验证的断言；
-- 少量样例；
-- 随机测试；
-- 超时；
-- 没有发现问题；
-- 未核对条件的外部引用；
-- 只在理想接口中成立的结果；
-- 未完成的证明草图；
-- 代码运行成功但没有验证其语义。
-
----
+```text
+exhaustive bounded random approximate timeout interrupted unresolved certificate-backed
+```
 
-## 16. 状态提升条件
+## 10. 状态提升
 
-### 16.1 提升为 `verified`
+状态必须反映证据强度，而不是语气或信心。
 
-必须同时满足：
+提升为 `verified` 必须满足：
 
-- 陈述精确；
-- 适用范围明确；
+- 陈述和范围精确；
 - 依赖已验证；
 - 证据完整；
 - 验收标准全部满足；
 - 相关测试或审计通过；
-- 没有开放义务；
-- 没有循环依赖；
-- 已记录限制与非结论。
-
-### 16.2 提升为 `verified-conditional`
-
-必须：
-
-- 明确列出额外条件；
-- 说明项目对象是否已知满足这些条件；
-- 不把条件结果写成一般结果；
-- 不关闭仍依赖入口条件的原问题。
-
-### 16.3 登记为 `observed`
-
-必须写明计算、样例或实验范围。  
-不得从观察直接推广到一般结论。
-
-### 16.4 关闭问题
-
-只有完全满足原问题的 `Answer criterion` 才能关闭。
-
-不能因为：
-
-- 完成有限范围；
-- 完成特殊情形；
-- 得到候选方案；
-- 代码通过小规模测试；
-- 在额外条件下成立；
-
-而关闭一般问题。
-
----
-
-## 17. 知识条目模板
-
-知识条目建议包含：
-
-```markdown
-# 标题
-
-- **ID:** K-xxxx
-- **Status:** verified
-- **Statement:** 精确陈述
-- **Scope:** 适用条件与边界
-- **Dependencies:** K-xxxx, K-yyyy
-- **Evidence:** `evidence/...`
-- **Mechanism:** 简短说明为何成立
-- **Caveats:** 限制和易误用之处
-- **Updated:** YYYY-MM-DD
-```
-
-完整证明、实现或分析应放在独立文件中。
-
-注册表不应只是结论列表，也不应成为完整证明合集。
-
----
-
-## 18. 开放问题模板
-
-```markdown
-# 问题标题
-
-- **ID:** Q-xxxx
-- **Status:** open
-- **Question:** 可独立回答的问题
-- **Why it matters:** 阻塞什么
-- **Known:** 已确认内容
-- **Missing:** 仍缺什么
-- **Answer criterion:** 何时可以关闭
-- **Next action:** 一个具体动作
-```
-
-问题解决后应原地更新状态并指向证据，不应直接删除。
-
----
-
-## 19. 失败记录模板
-
-```markdown
-# 方法标题
-
-- **ID:** A-xxxx
-- **Goal:** 原目标
-- **Approach:** 核心方法
-- **Failure type:** logical / counterexample / timeout / unsupported assumption / ...
-- **Failure point:** 最具体断点
-- **Why it failed:** 已确认原因
-- **Failure signature:** 用于识别换名重复
-- **Evidence:** 证据位置
-- **Retry conditions:** 何时值得重试
-- **Do not repeat:** 哪些表面变化仍属同一路线
-```
-
-注意：
-
-- 超时不表示对象或命题错误；
-- 工具报错不表示方案在原理上错误；
-- 暂时未完成不等于已经失败；
-- 未发现结果不等于反例不存在。
-
----
+- 没有开放义务或循环依赖；
+- 限制和非结论已记录。
 
-## 20. 最终结论的组装
+条件结果必须标为 `verified-conditional`，并保留未证明的入口。有限实验只能标为
+`observed`。只有完全满足原问题的 answer criterion 才能关闭问题。
 
-使用已有知识构建更大结论时，不能只列出条目 ID。
+## 11. 当前 handoff
 
-必须逐个检查：
+`HANDOFF_CURRENT.md` 是当前状态视图，不是 changelog。
 
-- 当前对象是否满足前提；
-- 输入输出是否匹配；
-- 范围是否一致；
-- 量词是否兼容；
-- 依赖是否已验证；
-- 结论是否足够强；
-- 模块之间是否缺少连接步骤；
-- 是否存在循环依赖；
-- 是否引用已废弃或被否定条目。
-
-最终正式文本应展开成连续可读的论证或设计说明，而不是仅写：
-
-```text
-由 K-0012、K-0031 和 K-0042 可得。
-```
-
----
-
-## 21. 历史与原始材料
-
-原始来源和历史记录默认只读。
-
-发现历史错误时，应：
-
-- 新增失败记录；
-- 更新当前知识条目；
-- 添加勘误或审计文件；
-- 标记旧内容已被替代；
-- 保留原始历史。
-
-不得修改历史对话，使其看起来从未产生过错误结论。
+更新时必须：
 
----
+- 完整读取并与权威记录核对；
+- 原地替换过时陈述；
+- 删除已解决问题和失效动作；
+- 合并重复内容；
+- 只保留下一位参与者需要的信息；
+- 最后修改；
+- 不追加日期更新区块。
 
-## 22. Git 与工作树安全
+## 12. Git 与工作树安全
 
-除非用户明确要求，代理不得：
+除非用户明确要求，不得：
 
-- 自动提交；
-- 自动推送；
-- 创建或合并 Pull Request；
-- 创建 tag 或 release；
-- 执行强制 reset；
-- 清理未知文件；
+- 自动提交、推送、建 PR、建 tag 或 release；
+- 强制 reset 或清理未知文件；
 - 覆盖无关用户修改；
-- 批量重命名；
-- 全仓库格式化；
+- 批量重命名或全仓库格式化；
 - 修改原始来源；
-- 删除稳定 ID。
-
-若工作树已有与任务无关的修改，必须避开这些文件。
-
-若目标文件存在冲突，应停止写入并报告，不得覆盖。
-
----
-
-## 23. 验证失败
-
-若任何必需检查失败：
-
-- 不得提升状态；
-- 不得关闭问题；
-- 不得把 handoff 改成“已完成”；
-- 必须保留或恢复最后一个已验证状态；
-- 必须记录失败命令和错误；
-- 不得通过删除测试、降低断言或手改结果来消除失败。
-
-实现失败是实现问题，不自动意味着项目命题或设计被否定。
-
----
-
-## 24. Diff 审阅
-
-交付前必须审阅完整 diff，并检查：
-
-- 是否修改了允许路径之外的文件；
-- 是否删除或更改稳定 ID；
-- 是否扩大了陈述范围；
-- 是否把条件结果写成一般结果；
-- 是否把观察写成验证结论；
-- 是否手工修改生成结果；
-- 是否修改了来源文件；
-- 是否留下临时文件、缓存或绝对路径；
-- 是否更新了 handoff 但未同步权威状态；
-- 是否存在无关格式化。
-
-任何一项存在问题，都必须修正后再交付。
-
----
-
-## 25. 当前 handoff 模板
+- 删除或复用稳定 ID。
 
-```markdown
-# Current Handoff
+目标文件有冲突时停止写入并报告，不得覆盖。
 
-## 1. Objective
+## 13. 验证失败
 
-当前目标与成功标准。
+任何必需检查失败时：
 
-## 2. Current state
+- 不得提升状态或关闭问题；
+- 不得把 handoff 写成完成；
+- 保持或恢复最后一个已验证状态；
+- 记录失败命令和错误；
+- 不得通过删除测试、降低断言或手改结果消除失败。
 
-- 当前阶段：
-- 最近完成：
-- 当前阻塞：
-- 当前路线：
+## 14. 交付前检查
 
-## 3. Reliable inputs
+必须审阅完整 diff，确认：
 
-下一轮可以安全依赖的条目和证据。
+- 只修改允许路径；
+- 未删除稳定 ID；
+- 未扩大陈述范围；
+- 未把条件结果或观察写成一般结论；
+- 未手改生成结果；
+- 未修改原始来源；
+- 未留下缓存、临时文件或绝对路径；
+- handoff 与权威状态一致。
 
-## 4. Provisional findings
+## 15. 完成报告
 
-尚未完全验证的内容及其状态。
-
-## 5. Do not repeat
-
-相关失败及其重试条件。
-
-## 6. Open questions
-
-按优先级列出少量问题。
-
-## 7. Immediate next actions
-
-最多 3–5 项，每项有明确完成标准。
-
-## 8. Required reading
-
-下一位参与者开始前必须读取的文件。
-
-## 9. Integrity warnings
-
-冲突、未验证步骤、数据缺口和环境限制。
-```
-
----
-
-## 26. 完成报告
-
-每次写入任务的最终报告必须包含：
+最终报告必须包含：
 
 ```text
-基础版本：
-工作模式：
-处理范围：
-读取的权威文件：
-
-新增：
-更新：
-降级或否定：
-关闭或重开的问题：
-
-修改文件：
-生成产物：
-运行的检查：
-检查结果：
-
-权威状态变化：
-未解决问题：
-明确不包含的结论：
-
-Git diff --stat：
-是否提交：否
-建议下一步：
+Base version:
+Mode:
+Scope:
+Authoritative files read:
+Changes:
+Evidence added:
+Checks run:
+Results:
+Canonical status change:
+Unresolved issues:
+Non-goals:
+Diff stat:
+Committed: no
 ```
 
-若没有改变项目权威状态，必须明确写：
+没有提升权威状态时必须写：
 
 ```text
 No canonical status was promoted.
 ```
 
-不得声称运行了未实际运行的命令。
-
----
+## 16. 停止条件
 
-## 27. 停止条件
+出现以下任一情况时停止扩大范围：
 
-出现以下任一情况时，代理必须停止扩大修改范围：
-
-- 无法确定权威来源；
-- 无法写出验收标准；
+- 无法确定权威来源或验收标准；
 - 需要未经验证的关键前提；
 - 需要把观察当成一般结论；
-- 需要覆盖无关用户修改；
-- 无法区分历史和当前状态；
-- 新内容只是在换名重复旧失败；
-- 状态文件将先于证据更新；
+- 需要覆盖无关修改；
+- 新内容只是换名重复旧失败；
+- 状态将先于证据更新；
 - 测试无法区分旧错误和新行为；
-- 修改开始扩展到与任务无关的文件；
-- 无法指出修改解决了哪个明确问题。
-
-此时应回到更小、可验证的子任务。
-
----
-
-## 28. 最终原则
-
-整个流程可以概括为：
-
-```text
-读取权威状态
-→ 锁定范围
-→ 提取候选变化
-→ 语义查重
-→ 建立证据或实现
-→ 敌对审计
-→ 原地整理知识
-→ 最后同步状态
-→ 生成干净 handoff
-→ 审阅 diff
-```
-
-最重要的规则是：
+- 修改扩展到与任务无关的文件。
 
-> 证据先于状态；  
-> 当前视图原地整理；  
-> 历史交给 Git 和归档；  
-> 不确定性必须保留；  
-> 条件结果不得冒充一般结果；  
-> 对话不能自动成为项目事实。
+详细流程、模板和示例见 [`WORKFLOW.md`](WORKFLOW.md)。
diff --git aa/docs/PROJECT_STATE.yaml bb/docs/PROJECT_STATE.yaml
index 84385c2..fc2088c 100644
--- aa/docs/PROJECT_STATE.yaml
+++ bb/docs/PROJECT_STATE.yaml
@@ -5,6 +5,11 @@ source_baseline:
   commit: cfadd24b52546d4d5800c4a3c5a75a2add86f928
   short_commit: cfadd24
   date: 2026-07-28
+repository_restructure:
+  generation: v3.1
+  local_patch_validation: passed
+  github_actions_requirement: must-pass-after-merge
+  canonical_mathematical_status_promoted: false
 theorem:
   id: M0
   statement: "Delta(H) < (1/4-o(1)) b^2 implies an independent transversal"
diff --git ab/docs/changes/2026-07-28-acceptance-fix.md bb/docs/changes/2026-07-28-acceptance-fix.md
new file mode 100644
index 0000000..0c0da40
--- /dev/null
+++ bb/docs/changes/2026-07-28-acceptance-fix.md
@@ -0,0 +1,31 @@
+# Acceptance fix: packaging, documentation integrity, and protocol split
+
+- **Mode:** implementation
+- **Canonical mathematical status promotion:** none
+- **Scope:** repository engineering and documentation governance
+
+## Changes
+
+1. Add `pyproject.toml` and a test extra so editable installation works.
+2. Upgrade the CI workflow and install from the package metadata.
+3. Replace the duplicated long `agent.md` with a short mandatory protocol.
+4. Keep detailed explanations and templates in `WORKFLOW.md`.
+5. Add a repository check enforcing `agent.md <= 260` lines.
+6. Repair Markdown/LaTeX control-character corruption.
+7. Restore root compatibility pointers.
+8. Distinguish local patch validation from post-merge GitHub Actions status.
+9. Extend repository checks for packaging, CI, control characters, and protocol length.
+
+## Acceptance criteria
+
+```text
+python -m pip install -e ".[test]" --no-build-isolation
+python -m compileall -q src enumerate tools tests
+python -m pytest -q
+python tools/check_repository.py
+python tools/check_generated_artifacts.py
+```
+
+GitHub Actions must be green after the patch is merged.
+
+No canonical mathematical status was promoted.
diff --git aa/evidence/audits/VALIDATION.md bb/evidence/audits/VALIDATION.md
index 5144760..a1634e5 100644
--- aa/evidence/audits/VALIDATION.md
+++ bb/evidence/audits/VALIDATION.md
@@ -1,43 +1,41 @@
 # Validation record
 
-## Baseline
+## Scope
 
-- Source commit: `cfadd24b52546d4d5800c4a3c5a75a2add86f928`
-- Validation date: 2026-07-28
-- Canonical status promotion: none
+This record describes validation of the acceptance-fix patch in a clean local
+worktree. It does not claim that GitHub Actions has passed before the patch is
+merged. The remote workflow must be green on the resulting commit.
 
-## Commands and results
+## Source baseline
+
+- Source material baseline: `cfadd24b52546d4d5800c4a3c5a75a2add86f928`
+- Patch validation date: 2026-07-28
+- Canonical mathematical status promotion: none
+
+## Commands
 
 ```text
+python -m pip install -e ".[test]" --no-build-isolation
 python -m compileall -q src enumerate tools tests
-result: passed
-
 python -m pytest -q
-result: 14 passed
-
 python tools/check_repository.py
-result: repository consistency checks passed
-
 python tools/check_generated_artifacts.py
-result: generated artifact checks passed
-
 python enumerate/q0015_configuration_auditor.py \
   --regressions-only \
   --generated-at 2026-07-28T00:00:00Z \
-  --output-dir /tmp/v3_run
-result: passed
+  --output-dir artifacts/runs/q0015
 ```
 
-## Regression summary
+## Acceptance requirements
 
-- root groups with failures: 144;
-- zero-error budget feasible: 48;
-- positive root-budget gap: 48;
-- no-configuration: 48;
-- specified `t_min`: 2.0;
-- forbidden genealogy merge `eta`: 1.0;
-- payload SHA-256: `ef02bf6446618026052c1a107913e4d015e3c8f3b48a59441a72afa09429a8ad`.
+- editable installation succeeds from `pyproject.toml`;
+- all tests pass;
+- repository consistency checks pass;
+- generated artifact checks pass;
+- the deterministic Q-0015 regression payload remains unchanged;
+- the GitHub Actions workflow is green after merge.
 
-## Limitations
+## Mathematical scope
 
-The validation establishes repository consistency and reproducibility of the committed finite regression baseline. It does not prove any currently open mathematical question.
+These checks establish repository consistency and finite-baseline reproducibility.
+They do not prove any open mathematical question.
diff --git aa/history/CHANGELOG.md bb/history/CHANGELOG.md
index 0cf940c..c1eeb66 100644
--- aa/history/CHANGELOG.md
+++ bb/history/CHANGELOG.md
@@ -13,3 +13,11 @@
 - introduced importable `src/hypergraph_il/` package;
 - separated capacity ledger types;
 - added Q-0015 regression tests, certificate schema, CI, and repository checks.
+
+## 2026-07-28 — Acceptance fix
+
+- shortened `agent.md` and moved details to `WORKFLOW.md`;
+- added Python packaging metadata and repaired CI installation;
+- repaired Markdown escape corruption and restored compatibility pointers;
+- added machine checks for protocol length, control characters, packaging, and CI configuration;
+- no canonical mathematical status was promoted.
diff --git ab/pyproject.toml bb/pyproject.toml
new file mode 100644
index 0000000..38efbf8
--- /dev/null
+++ bb/pyproject.toml
@@ -0,0 +1,32 @@
+[build-system]
+requires = ["setuptools>=69"]
+build-backend = "setuptools.build_meta"
+
+[project]
+name = "hypergraph-il-audit"
+version = "0.3.0"
+description = "Auditable proof-engineering tools for partitioned hypergraph independent transversals"
+requires-python = ">=3.12"
+dependencies = [
+  "networkx==3.6.1",
+  "numpy==2.3.5",
+  "scipy==1.17.0",
+]
+
+[project.optional-dependencies]
+test = [
+  "pytest==8.4.2",
+]
+
+[project.scripts]
+q0015-audit = "hypergraph_il.cli:main"
+
+[tool.setuptools]
+package-dir = {"" = "src"}
+
+[tool.setuptools.packages.find]
+where = ["src"]
+
+[tool.pytest.ini_options]
+testpaths = ["tests"]
+addopts = "-ra"
diff --git aa/tools/check_repository.py bb/tools/check_repository.py
index 306bc12..aa7dfbe 100644
--- aa/tools/check_repository.py
+++ bb/tools/check_repository.py
@@ -1,6 +1,7 @@
 #!/usr/bin/env python3
 from __future__ import annotations
 
+import hashlib
 import json
 import re
 from pathlib import Path
@@ -19,10 +20,12 @@ def require(path: str) -> Path:
 
 required = [
     "README.md", "AGENTS.md", "agent.md", "WORKFLOW.md", "HANDOFF_CURRENT.md",
+    "pyproject.toml", ".github/workflows/enumerate-smoke.yml",
+    "FACTS.md", "FAILURES.md", "QUESTIONS.md",
+    "PIVOT_SWITCH_ESCAPE_FRAMEWORK.md", "SINGLE_DEFECT_FRAMEWORK.md",
     "knowledge/FACTS.md", "knowledge/FAILURES.md", "knowledge/QUESTIONS.md",
     "knowledge/DECISIONS.md", "knowledge/DEFINITIONS.md",
     "docs/PROJECT_STATE.yaml", "docs/PROOF_DAG.md", "docs/QUICKSTART_10_MINUTES.md",
-    "docs/changes/2026-07-28-general-workflow-restructure.md",
     "evidence/audits/REPOSITORY_AUDIT.md",
     "evidence/experiments/q0015/baselines/q0015_audit_results.json",
     "evidence/experiments/q0015/MANIFEST.json",
@@ -34,13 +37,21 @@ for item in required:
     require(item)
 
 
-# Generic protocol must not contain project-specific status.
-agent_text = require("agent.md").read_text(encoding="utf-8")
+agent_path = require("agent.md")
+agent_text = agent_path.read_text(encoding="utf-8")
+agent_lines = len(agent_text.splitlines())
+if agent_lines > 260:
+    ERRORS.append(f"agent.md is too long: {agent_lines} lines (maximum 260)")
+if "WORKFLOW.md" not in agent_text:
+    ERRORS.append("agent.md must link the detailed WORKFLOW.md")
 for token in ["Q-0015", "Q-0016", "Q-0017", "one-quarter theorem", "genealogy"]:
     if token in agent_text:
         ERRORS.append(f"generic agent.md contains project-specific token: {token}")
 
-# Compatibility files must remain pointers, not duplicate authorities.
+workflow_lines = len(require("WORKFLOW.md").read_text(encoding="utf-8").splitlines())
+if workflow_lines > 500:
+    ERRORS.append(f"WORKFLOW.md is too long: {workflow_lines} lines (maximum 500)")
+
 for pointer, canonical in [
     ("FACTS.md", "knowledge/FACTS.md"),
     ("FAILURES.md", "knowledge/FAILURES.md"),
@@ -50,6 +61,44 @@ for pointer, canonical in [
     if canonical not in text or len(text.splitlines()) > 8:
         ERRORS.append(f"{pointer} is not a short compatibility pointer")
 
+for pointer in ["PIVOT_SWITCH_ESCAPE_FRAMEWORK.md", "SINGLE_DEFECT_FRAMEWORK.md"]:
+    if len(require(pointer).read_text(encoding="utf-8").splitlines()) > 10:
+        ERRORS.append(f"{pointer} is not a short compatibility pointer")
+
+# Canonical Markdown must not contain hidden control characters introduced by
+# incorrectly escaped generator strings.
+canonical_paths = [
+    ROOT / "README.md", ROOT / "AGENTS.md", ROOT / "agent.md", ROOT / "WORKFLOW.md",
+    ROOT / "HANDOFF_CURRENT.md",
+    *sorted((ROOT / "knowledge").glob("*.md")),
+    *sorted((ROOT / "docs").rglob("*.md")),
+    *sorted((ROOT / "evidence").rglob("*.md")),
+]
+for path in canonical_paths:
+    text = path.read_text(encoding="utf-8")
+    bad = [(index, ord(char)) for index, char in enumerate(text)
+           if ord(char) < 32 and char not in "\n"]
+    if bad:
+        ERRORS.append(
+            f"control character in {path.relative_to(ROOT)}: "
+            + ", ".join(f"offset {index}=0x{code:02x}" for index, code in bad[:5])
+        )
+
+readme_text = require("README.md").read_text(encoding="utf-8")
+for expected in [r"\frac14", r"\right", r"\text{", r"\to"]:
+    if expected not in readme_text:
+        ERRORS.append(f"README.md missing intact LaTeX token: {expected}")
+
+ci_text = require(".github/workflows/enumerate-smoke.yml").read_text(encoding="utf-8")
+for expected in ['actions/checkout@v7', 'actions/setup-python@v7', 'pip install -e ".[test]"']:
+    if expected not in ci_text:
+        ERRORS.append(f"CI workflow missing: {expected}")
+
+pyproject_text = require("pyproject.toml").read_text(encoding="utf-8")
+for expected in ["[build-system]", "[project]", "[project.optional-dependencies]", "[tool.setuptools.packages.find]"]:
+    if expected not in pyproject_text:
+        ERRORS.append(f"pyproject.toml missing section: {expected}")
+
 state_text = require("docs/PROJECT_STATE.yaml").read_text(encoding="utf-8")
 for expected in [
     "commit: cfadd24b52546d4d5800c4a3c5a75a2add86f928",
@@ -68,7 +117,6 @@ if re.search(r"Q-0017.{0,50}(closed|已证明|已关闭)", handoff, re.I):
 if re.search(r"^## .*update", handoff, re.I | re.M):
     ERRORS.append("handoff contains append-only update sections")
 
-# Stable IDs must remain.
 registries = "\n".join(require(path).read_text(encoding="utf-8") for path in [
     "knowledge/FACTS.md", "knowledge/FAILURES.md", "knowledge/QUESTIONS.md"
 ])
@@ -76,7 +124,6 @@ for token in ["F-0035", "F-0036", "A-0025", "A-0026", "Q-0015", "Q-0016", "Q-001
     if token not in registries:
         ERRORS.append(f"stable registry ID disappeared: {token}")
 
-# Raw source files are represented in the manifest.
 source_manifest = json.loads(require("sources/raw/MANIFEST.json").read_text(encoding="utf-8"))
 manifested_entries = {entry["path"]: entry for entry in source_manifest.get("files", [])}
 for path in (ROOT / "sources/raw/conversations").glob("*"):
@@ -87,27 +134,20 @@ for path in (ROOT / "sources/raw/conversations").glob("*"):
     if entry is None:
         ERRORS.append(f"raw source missing from manifest: {path.relative_to(ROOT)}")
         continue
-    import hashlib
     actual = hashlib.sha256(path.read_bytes()).hexdigest()
     if entry.get("sha256") != actual or entry.get("bytes") != path.stat().st_size:
         ERRORS.append(f"raw source manifest mismatch: {path.relative_to(ROOT)}")
 
-# Generated baseline must use the artifact envelope.
-baseline = json.loads(require("evidence/experiments/q0015/baselines/q0015_audit_results.json").read_text(encoding="utf-8"))
+baseline = json.loads(
+    require("evidence/experiments/q0015/baselines/q0015_audit_results.json")
+    .read_text(encoding="utf-8")
+)
 if baseline.get("metadata", {}).get("schema_version") != "research-artifact-v1":
     ERRORS.append("baseline does not use research-artifact-v1")
 if baseline.get("metadata", {}).get("result_type") != "exhaustive-regression":
     ERRORS.append("baseline result type is not exhaustive-regression")
 
-# Local Markdown links in current/canonical documents.
 link_pattern = re.compile(r"\[[^\]]*\]\(([^)]+)\)")
-canonical_paths = [
-    ROOT / "README.md", ROOT / "AGENTS.md", ROOT / "agent.md", ROOT / "WORKFLOW.md",
-    ROOT / "HANDOFF_CURRENT.md",
-    *sorted((ROOT / "knowledge").glob("*.md")),
-    *sorted((ROOT / "docs").rglob("*.md")),
-    *sorted((ROOT / "evidence").rglob("*.md")),
-]
 for path in canonical_paths:
     text = path.read_text(encoding="utf-8")
     for raw_target in link_pattern.findall(text):
