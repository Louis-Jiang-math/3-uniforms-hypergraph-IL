# Historical material

This directory contains superseded or monolithic project documents. Git and raw sources preserve history; current project state is defined elsewhere.

- `legacy-frameworks/`: former root-level monolithic framework documents;
- `CHANGELOG.md`: delivery and structural changes.

Do not cite a historical document as current status without checking `knowledge/`, `docs/PROOF_DAG.md`, and `HANDOFF_CURRENT.md`.
