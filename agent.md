# AGENT.md — 通用仓库写入协议

本文件是 AI、自动化代理和人类辅助工具修改本仓库时必须遵守的最小强制协议。
详细解释、模板和示例见 [`WORKFLOW.md`](WORKFLOW.md)；项目专有规则见
[`AGENTS.md`](AGENTS.md)。

## 1. 优先级与权威来源

必须按以下顺序处理冲突：

1. 用户当前明确指令；
2. 当前目录适用的项目规则；
3. 本文件；
4. 当前权威知识、证据和状态文件；
5. 历史资料、旧对话和实验草稿。

对话、AI 回答、用户猜测、有限样例和未审计日志只能产生候选变化，不能自动成为项目事实。

必须区分：

- 原始来源：默认只读；
- 知识注册：保存当前可依赖的陈述及范围；
- 证据与实现：保存证明、代码、测试、分析和证书；
- 当前状态：保存目标、阻塞和下一步；
- 历史：由 Git、归档、审计和日志保存。

## 2. 工作模式

每次任务必须声明一种模式：

- `read-only`：只读取和审计；
- `proposal`：只生成方案或补丁；
- `implementation`：修改实现、测试、证据或文档；
- `status-promotion`：允许改变权威条目或项目状态。

`implementation` 不自动包含 `status-promotion`。

## 3. 写前范围锁定

第一次修改前必须记录：

```text
Task:
Mode:
Base version:
Target:
Inputs:
Outputs:
Allowed paths:
Forbidden paths:
Expected status change:
Acceptance criteria:
Required checks:
Non-goals:
```

无法确定基础版本时必须如实说明。没有允许路径和验收标准时，不得进行大规模写入。

## 4. 开始前读取

按顺序读取：

1. `AGENTS.md` 或当前目录适用的项目规则；
2. `README.md`；
3. 当前 handoff 或状态文件；
4. 相关知识、失败和问题条目；
5. 相关证据、实现和测试；
6. 必要的原始来源。

不得仅凭上一轮对话记忆继续工作。共享流程或外部 skill 必须显式读取。

## 5. 候选变化与查重

写回对话结果前，先分类：

```text
ADD UPDATE CONFIRM DOWNGRADE REFUTE SUPERSEDE MERGE CLOSE REOPEN NO_CHANGE
```

每项必须说明影响对象、依据、目标文件、是否改变权威状态以及是否需要人工复核。

新增条目前必须进行语义查重，比较对象、前提、范围、结论、机制和证据；
不得只比较标题或关键词。

## 6. 文件更新语义

默认规则：

| 文件类型 | 更新方式 |
|---|---|
| 事实、需求、决策、问题 | 原地结构化更新 |
| 证明、分析、设计说明 | 原地整理；Git 保存旧版本 |
| 失败记录 | 新失败新增；已有失败原地补充 |
| 审计和实验 | 通常新建独立记录 |
| CHANGELOG | 追加 |
| 当前 handoff | 重写为当前快照 |
| 原始来源 | 只读 |
| 机器结果 | 由脚本重新生成 |

不得用文件尾部的“补充、最新修正、再次修正”代替原地修改。

## 7. 证据优先的事务顺序

必须按以下顺序写入：

1. 测试、验收用例或反例；
2. 实现、分析或证明；
3. 机器输出、证书、日志和哈希；
4. 事实、失败、问题和决策；
5. 主题或框架文档；
6. 项目状态；
7. 当前 handoff；
8. 进展记录和完成报告。

证据层未通过时，不得提升状态、关闭问题或把 handoff 改成“已完成”。

## 8. 补丁规则

必须：

- 先读取目标文件相关部分；
- 优先使用最小语义补丁；
- 保留稳定 ID 和公共接口；
- 删除被替代的当前表述；
- 修改后审阅完整 diff；
- 对大规模重构提供迁移说明。

不得：

- 覆盖未完整读取的长文件；
- 使用宽泛替换批量修改状态或编号；
- 静默删除仍被引用的内容；
- 因格式统一改变含义；
- 修改与任务无关的文件。

## 9. 测试、机器结果与验证

修改实现时必须新增或同步修改测试，并覆盖失败路径和非法输入。

机器结果必须记录生成器、命令、输入、参数、随机种子、依赖版本、基础版本、
结果类型和 SHA-256。必须先写临时文件，验证成功后再原子替换目标；
不得手工编辑生成结果使测试通过。

常见结果类型必须明确区分：

```text
exhaustive bounded random approximate timeout interrupted unresolved certificate-backed
```

## 10. 状态提升

状态必须反映证据强度，而不是语气或信心。

提升为 `verified` 必须满足：

- 陈述和范围精确；
- 依赖已验证；
- 证据完整；
- 验收标准全部满足；
- 相关测试或审计通过；
- 没有开放义务或循环依赖；
- 限制和非结论已记录。

条件结果必须标为 `verified-conditional`，并保留未证明的入口。有限实验只能标为
`observed`。只有完全满足原问题的 answer criterion 才能关闭问题。

## 11. 当前 handoff

`HANDOFF_CURRENT.md` 是当前状态视图，不是 changelog。

更新时必须：

- 完整读取并与权威记录核对；
- 原地替换过时陈述；
- 删除已解决问题和失效动作；
- 合并重复内容；
- 只保留下一位参与者需要的信息；
- 最后修改；
- 不追加日期更新区块。

## 12. Git 与工作树安全

除非用户明确要求，不得：

- 自动提交、推送、建 PR、建 tag 或 release；
- 强制 reset 或清理未知文件；
- 覆盖无关用户修改；
- 批量重命名或全仓库格式化；
- 修改原始来源；
- 删除或复用稳定 ID。

目标文件有冲突时停止写入并报告，不得覆盖。

## 13. 验证失败

任何必需检查失败时：

- 不得提升状态或关闭问题；
- 不得把 handoff 写成完成；
- 保持或恢复最后一个已验证状态；
- 记录失败命令和错误；
- 不得通过删除测试、降低断言或手改结果消除失败。

## 14. 交付前检查

必须审阅完整 diff，确认：

- 只修改允许路径；
- 未删除稳定 ID；
- 未扩大陈述范围；
- 未把条件结果或观察写成一般结论；
- 未手改生成结果；
- 未修改原始来源；
- 未留下缓存、临时文件或绝对路径；
- handoff 与权威状态一致。

## 15. 完成报告

最终报告必须包含：

```text
Base version:
Mode:
Scope:
Authoritative files read:
Changes:
Evidence added:
Checks run:
Results:
Canonical status change:
Unresolved issues:
Non-goals:
Diff stat:
Committed: no
```

没有提升权威状态时必须写：

```text
No canonical status was promoted.
```

## 16. 停止条件

出现以下任一情况时停止扩大范围：

- 无法确定权威来源或验收标准；
- 需要未经验证的关键前提；
- 需要把观察当成一般结论；
- 需要覆盖无关修改；
- 新内容只是换名重复旧失败；
- 状态将先于证据更新；
- 测试无法区分旧错误和新行为；
- 修改扩展到与任务无关的文件。

详细流程、模板和示例见 [`WORKFLOW.md`](WORKFLOW.md)。
