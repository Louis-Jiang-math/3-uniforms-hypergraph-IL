# Project instructions

## Mathematical status

- The one-quarter theorem is open.
- The active proof strategy is **fork--inverse-fiber--anchor**.
- The single active proof node is `S1 / Q-0019`: global inverse-fiber
  decomposition.
- Route B and Route A are supporting/suspended respectively; do not silently
  reactivate either as the main route.

## Mandatory distinctions

1. A finite actual-history LP organizes transient and recurrent behavior; it
   does not create a degree lower bound.
2. A local heavy inverse fiber is not a persistent anchor.
3. A deterministic-policy return is not an all-release core.
4. A partial independent completion is not a global independent transversal.
5. Fixed-instance exhaustion is not a uniform asymptotic estimate.
6. Occurrence history, actual edge identity, owner and root provenance may not
   be discarded to manufacture multiplicity bounds or negative terms.

## Active mathematical object

For pivot `x`, work with actual disjoint-blocker fork occurrences and their
loads

\[
2F_x\le d_H(x)\ell_x^*.
\]

Progress must either prove a uniform `O(b^{-2})` bound for the diffuse load or
extract a future-complete fixed anchor handled by F-0058. The unresolved
one-coordinate migration term may not be renamed as a terminal.

## Claim discipline

- Use `knowledge/FACTS.md`, `knowledge/FAILURES.md`,
  `knowledge/QUESTIONS.md`, and `knowledge/DECISIONS.md` as canonical
  registries.
- Preserve stable IDs.
- State every hypothesis, especially faithfulness, future completeness,
  minimality, and uniformity.
- Computational output is evidence, never an asymptotic theorem without a
  finite-witness or compactness argument.
- Historical evidence files do not override current status documents.

## Repository discipline

Read `agent.md` for the short write protocol and `WORKFLOW.md` for detailed
formats. Run compileall, pytest, repository consistency, and generated-artifact
checks before delivery.
