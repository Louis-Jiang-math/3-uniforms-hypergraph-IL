# Progress ledger

## Current route

The active strategy is fork--inverse-fiber--anchor.

### Closed supporting steps

- finite actual-history potential/core equivalence (F-0052);
- fixed-pivot target-following under future-complete persistence (F-0058);
- canonical full-target repair (F-0090);
- canonical maximal-matching repair and fork-density dichotomy (F-0091);
- two-coordinate replacement-box multiplicity (F-0092).

### Scope correction

F-0078 is retained only as a fixed-instance history-unfolded stopping identity.
Its zero-set closure and automatic return-to-core interpretation are withdrawn.

### Active step

Q-0019 must globalize inverse-fiber codimension while retaining actual history
and preventing duplicated mass. In particular it must control migration of the
one-coordinate heavy pair.

The one-quarter theorem, Q-0016, Q-0017, Q-0018 and Q-0019 remain open.
